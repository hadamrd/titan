import { ComingSoon } from './ComingSoon'

export function NotificationsSection() {
  return (
    <ComingSoon
      title="Notifications"
      detail="Email digests and Slack delivery are tracked alongside the notifications service. Wiring lands with the GET /api/v1/notifications endpoint."
    />
  )
}
