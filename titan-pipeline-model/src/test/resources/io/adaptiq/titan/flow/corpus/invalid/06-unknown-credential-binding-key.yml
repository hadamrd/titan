# Invalid: unknown key inside a credentialBinding object — that $def is closed.
stages:
  - stage: Build
    credentials:
      - id: tok
        type: string
        variable: TOKEN
        bogusVariable: NOPE
    steps:
      - sh: make
