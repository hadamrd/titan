# GOLDEN INVALID — a credential binding with an unknown key `envVar` (the
# grammar key is `variable`). The credentialBinding $def is closed.
stages:
  - stage: Deploy
    credentials:
      - id: token
        type: string
        envVar: TOKEN
    steps:
      - sh: ./deploy.sh
