apiVersion: v2
name: titan
description: |
  Standalone Titan k3s deployment — postgres + keycloak + titan-server +
  titan-ui + titan-worker. The Kubernetes-shaped counterpart of the local
  docker rig under rig/local/docker-compose.yml. Standalone.
type: application
version: 1.0.0-rc1
appVersion: "1.0.0-rc1"
maintainers:
  - name: Titan contributors
