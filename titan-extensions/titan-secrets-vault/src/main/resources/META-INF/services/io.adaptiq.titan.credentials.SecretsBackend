# Titan SecretsBackend SPI (design/41 §8.6) — discovered by java.util.ServiceLoader.
# Dropping this jar on the controller classpath, plus TITAN_SECRETS_BACKEND=vault,
# is the whole installation: no engine change. Skeleton today (see #475 — every
# write method throws UnsupportedOperationException pointing at the follow-up
# ticket that will land it).
io.adaptiq.titan.secrets.vault.VaultSecretsBackend
